name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64 — musl static via cargo-zigbuild (avoids wasmer __rust_probestack linker issue)
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            name: linux-x86_64
            use_zigbuild: true
          # Linux ARM64 — musl static (cross works fine for aarch64, no probestack on ARM)
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            name: linux-aarch64
            use_cross: true
          # macOS Apple Silicon (M1/M2/M3/M4)
          - target: aarch64-apple-darwin
            os: macos-14
            name: macos-aarch64
          # Windows x86_64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            name: windows-x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            claw-node/target/
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('claw-node/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Install cargo-zigbuild
        if: matrix.use_zigbuild
        run: |
          pip3 install ziglang
          cargo install cargo-zigbuild

      - name: Install cross
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build (zigbuild — Linux x86_64 musl static)
        if: matrix.use_zigbuild
        working-directory: claw-node
        run: cargo zigbuild --release --target ${{ matrix.target }} -p claw-node

      - name: Build (cross — Linux ARM64 musl)
        if: matrix.use_cross
        working-directory: claw-node
        run: cross build --release --target ${{ matrix.target }} -p claw-node

      - name: Build (native — macOS / Windows)
        if: "!matrix.use_cross && !matrix.use_zigbuild"
        working-directory: claw-node
        env:
          MACOSX_DEPLOYMENT_TARGET: ${{ contains(matrix.target, 'aarch64-apple') && '11.0' || '' }}
        run: cargo build --release --target ${{ matrix.target }} -p claw-node

      - name: Verify binary (Unix, non-cross)
        if: runner.os != 'Windows' && !matrix.use_cross
        run: |
          file claw-node/target/${{ matrix.target }}/release/claw-node
          # Only run --version on native arch (zigbuild produces static binary for same arch)
          if [ "${{ matrix.use_zigbuild }}" != "true" ]; then
            claw-node/target/${{ matrix.target }}/release/claw-node --version
          fi

      - name: Verify static linking (cross)
        if: matrix.use_cross
        run: |
          file claw-node/target/${{ matrix.target }}/release/claw-node

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          cd claw-node/target/${{ matrix.target }}/release
          tar czf ../../../../claw-node-${{ matrix.name }}.tar.gz claw-node
          cd ../../../..

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: |
          cd claw-node/target/${{ matrix.target }}/release
          7z a ../../../../claw-node-${{ matrix.name }}.zip claw-node.exe
          cd ../../../..

      - uses: actions/upload-artifact@v4
        with:
          name: claw-node-${{ matrix.name }}
          path: claw-node-*.*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Generate checksums
        run: sha256sum claw-node-*.tar.gz claw-node-*.zip > SHA256SUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Install

            **Linux** (x86_64, static binary — works on any distro):
            ```bash
            curl -L https://github.com/clawlabz/claw-network/releases/download/${{ github.ref_name }}/claw-node-linux-x86_64.tar.gz | tar xz
            sudo mv claw-node /usr/local/bin/
            claw-node --version
            ```

            **Linux ARM64**:
            ```bash
            curl -L https://github.com/clawlabz/claw-network/releases/download/${{ github.ref_name }}/claw-node-linux-aarch64.tar.gz | tar xz
            sudo mv claw-node /usr/local/bin/
            ```

            **macOS** (Apple Silicon):
            ```bash
            curl -L https://github.com/clawlabz/claw-network/releases/download/${{ github.ref_name }}/claw-node-macos-aarch64.tar.gz | tar xz
            sudo mv claw-node /usr/local/bin/
            ```

            **Docker**:
            ```bash
            docker run -d -p 9710:9710 -p 9711:9711 ghcr.io/clawlabz/claw-node:latest
            ```

            **Verify checksums**:
            ```bash
            sha256sum --check SHA256SUMS.txt
            ```
          files: |
            claw-node-*.tar.gz
            claw-node-*.zip
            SHA256SUMS.txt
